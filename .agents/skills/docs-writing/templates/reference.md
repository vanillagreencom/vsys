# <Reference subject>

<What this reference answers.>

<Include the canonical schema or pattern below when this file owns it. Remove the block for a value-only reference.>

```text
<Canonical schema or pattern>
```

| Item | Meaning |
|---|---|
| `<name>` | <shape and meaning> |

<Add a Default column only where defaults are defined.>
