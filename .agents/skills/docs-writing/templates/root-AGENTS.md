# <repo>

<What the repository is, in two or three sentences.>

## Commands

- `<command>`: <what it does, only where a reader could not find it from the tooling itself>.

## Conventions

- <A rule that differs from the tool default or the language norm, one line each.>

## Read next

- `docs/architecture/overview.md`: before structural work.
- `<dir>/AGENTS.md`: when working under `<dir>/`.
