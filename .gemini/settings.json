{
  "hooks": {
    "BeforeTool": [
      {
        "matcher": "run_shell_command",
        "hooks": [
          {
            "type": "command",
            "command": "p='.gemini/hooks/block-argv-kill.sh'; r=$(cd -P . && pwd); case $r in /*) ;; *) r=;; esac; while [ -n \"$r\" ] && ! [ -f \"$r/$p\" ]; do [ \"$r\" = / ] && r= || { r=${r%/*}; [ -n \"$r\" ] || r=/; }; done; [ -n \"$r\" ] || { echo \"kendex: no directory above $PWD holds $p; run kendex refresh in the project\" >&2; exit 1; }; bash \"$r/$p\"",
            "timeout": 10000
          },
          {
            "type": "command",
            "command": "p='.gemini/hooks/block-bare-cd.sh'; r=$(cd -P . && pwd); case $r in /*) ;; *) r=;; esac; while [ -n \"$r\" ] && ! [ -f \"$r/$p\" ]; do [ \"$r\" = / ] && r= || { r=${r%/*}; [ -n \"$r\" ] || r=/; }; done; [ -n \"$r\" ] || { echo \"kendex: no directory above $PWD holds $p; run kendex refresh in the project\" >&2; exit 1; }; bash \"$r/$p\""
          },
          {
            "type": "command",
            "command": "p='.gemini/hooks/block-repo-copy.sh'; r=$(cd -P . && pwd); case $r in /*) ;; *) r=;; esac; while [ -n \"$r\" ] && ! [ -f \"$r/$p\" ]; do [ \"$r\" = / ] && r= || { r=${r%/*}; [ -n \"$r\" ] || r=/; }; done; [ -n \"$r\" ] || { echo \"kendex: no directory above $PWD holds $p; run kendex refresh in the project\" >&2; exit 1; }; bash \"$r/$p\""
          },
          {
            "type": "command",
            "command": "p='.gemini/hooks/block-worktree-refresh.sh'; r=$(cd -P . && pwd); case $r in /*) ;; *) r=;; esac; while [ -n \"$r\" ] && ! [ -f \"$r/$p\" ]; do [ \"$r\" = / ] && r= || { r=${r%/*}; [ -n \"$r\" ] || r=/; }; done; [ -n \"$r\" ] || { echo \"kendex: no directory above $PWD holds $p; run kendex refresh in the project\" >&2; exit 1; }; bash \"$r/$p\"",
            "timeout": 10000
          },
          {
            "type": "command",
            "command": "p='.gemini/hooks/command-safety.sh'; r=$(cd -P . && pwd); case $r in /*) ;; *) r=;; esac; while [ -n \"$r\" ] && ! [ -f \"$r/$p\" ]; do [ \"$r\" = / ] && r= || { r=${r%/*}; [ -n \"$r\" ] || r=/; }; done; [ -n \"$r\" ] || { echo \"kendex: no directory above $PWD holds $p; run kendex refresh in the project\" >&2; exit 1; }; bash \"$r/$p\"",
            "timeout": 10000
          },
          {
            "type": "command",
            "command": "p='.gemini/hooks/pre-commit-check.sh'; r=$(cd -P . && pwd); case $r in /*) ;; *) r=;; esac; while [ -n \"$r\" ] && ! [ -f \"$r/$p\" ]; do [ \"$r\" = / ] && r= || { r=${r%/*}; [ -n \"$r\" ] || r=/; }; done; [ -n \"$r\" ] || { echo \"kendex: no directory above $PWD holds $p; run kendex refresh in the project\" >&2; exit 1; }; bash \"$r/$p\"",
            "timeout": 60000
          }
        ]
      }
    ]
  }
}
